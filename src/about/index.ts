/**
 * About Feature Module
 * 
 * Clean module export interface for the About Page & Section.
 * This self-contained folder structure ensures zero-coupling with other pages,
 * making future deletion or toggling trivial.
 */

export { default, default as AboutPage } from './AboutPage';
export { default as AboutSection } from './AboutSection';
